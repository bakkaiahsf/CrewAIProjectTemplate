from setuptools import setup, find_packages

setup(
    name='crewai',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
    ],
    author='Your Name',
    description='A framework for CrewAI projects',
    include_package_data=True,
    python_requires='>=3.7',
) 